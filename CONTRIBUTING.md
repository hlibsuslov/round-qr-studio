# Contributing

1. Create a focused branch from `main`.
2. Keep QR safety margins and high error correction intact.
3. Run `npx tsc --noEmit` and `npm run build` before opening a pull request.
4. Describe user-visible behavior and include mobile considerations.
5. Do not commit generated build output, credentials, or environment files.
